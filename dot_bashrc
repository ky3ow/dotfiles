if command -v caparace
then
	source <(carapace _carapace bash)
fi

if command -v starship
then 
	eval "$(starship init bash)"
fi

function take {
	mkdir $1
	cd $1
}

alias alp="distrobox enter alpine"
alias v="nvim"

alias che="chezmoi edit"
alias chup="chezmoi update"

export EDITOR=nvim
